// Demo control. One instance lives in each demo room. Shortly after rm_a
// starts, it kicks off a fade transition to rm_b so `gmx run` shows the
// Swedish blue -> yellow blend. (Room background colours are set on each
// room's Background layer, so the captured surfaces already carry them.)
timer = 0;
